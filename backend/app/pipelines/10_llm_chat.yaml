name: LLM 对话
description: 用户输入提示词；如果包含链接会先抓取网页正文，再一起交给 Ollama 生成回答。

inputs:
  prompt:
    required: true
    multiline: true
    label: 提示词
    description: 发给 LLM 的内容；含 http(s) 链接时自动抓取网页正文作参考

nodes:
  网页抓取:
    type: web_fetch

  LLM回答:
    type: llm_chat
    depends_on: [网页抓取]
    timeout: 300
